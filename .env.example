# Copy to .env.local (or run `vercel env pull .env.local` once Neon is provisioned).

# Provided automatically once you run:
#   vercel integration add neon
DATABASE_URL=

# Any long random string. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
